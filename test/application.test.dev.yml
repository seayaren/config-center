server:
  port: 8200
spring:
  application:
    name: Manbing Config
  profiles:
    active: git #设置使用本地配置(默认是git,可以设置：subversion(SVN)，native(本地))
  cloud:
    config:
      server:
        #如下是GIT配置
        git:
          uri: https://github.com/seayaren/config-center
          search-paths: config
          username: 38428108@qq.com
          password: renjianbowq7890
          default-label: master

    consul:
      host: localhost
      port: 8500
      discovery:
        register: false
  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
sleuth:
  sampler:
    probability: 1.0

